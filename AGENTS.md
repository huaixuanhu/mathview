# MathView 人机协作规则

<!-- Generated/adapted from human-ai-governance v0.7.7 -->

## 1. 项目入口

- 当前项目为 Tier 2（持久、低影响项目）：浏览器内的概率分布教学与探索工具，当前代码没有账户、交易、支付或后台数据服务能力。按任务实际影响的能力评估风险；金融数学图表本身不提高权限等级。
- 项目结构、数学约定和运行命令以 [README.md](README.md) 及对应源码为准。
- 本次版本来源和接入记录见 [v0.7.7 接入记录](docs/human-ai-governance-v0.7.7.md)。这是项目内的规则适配；本机全局 skill 后续变化不自动升级本项目。
- 先读取与任务直接相关的文件，再按实际问题扩展检查范围。修改前查看 Git 状态，保留用户未提交的工作。

## 2. 沟通与事实

- 严格低幻觉：不得编造功能、来源、授权、运行结果或完成状态。不确定的内容明确说明。
- 外部、时效性事实联网核实；本地代码和运行状态优先依据源码、工具输出和测试。
- 使用通俗中文向用户解释技术工作。必要的学术术语使用英文原词并在首次出现时附中文解释；简单英文、代码、路径、命令和配置键保留原样。
- 默认采用 compact（简洁且足以判断）的沟通密度：先说明具体对象的当前状态，再保留关键证据、限制和下一步。需要完整说明时直接展开。
- 减少“不是……而是……”句式。不要用省略主语的状态碎片代替完整说明，不编造原因或责任人。
- plain（通俗技术表达）只改变措辞，不减少推理、实现、验证、证据或安全要求。正式计划和工程记录保留完整细节；面向读者的文章或演示内容按用途调整表达。
- 子代理的内部任务说明和报告可使用英文，无需重复主对话的双语表达要求；主代理负责整合证据和用户沟通。

## 3. 一致意见、授权与任务连续性

- 重要修改前简述范围、不涉及的内容、主要文件、验证和外部影响。用户的明确任务指令可以构成相应范围的一致意见，无需重复询问已明确的事项。
- 一次有效的一致意见覆盖范围内的安全实现、诊断、修复、验证和收尾。已经授权的外部操作，在对象、范围、限制和条件仍满足时继续执行。
- 例如，用户已要求“升级后直接 push”，完成相应修改和验证后可提交并进行普通 push，无需再次请求同一授权。
- 仅在范围实质扩大、操作超出已授权范围、存在影响结果的重要未决选择，或某项明确审批及续期条件仍未满足时暂停相关操作。先完成不受该问题影响的安全准备。
- 授权的有效期、单次限制、累计限制和明确前置条件继续有效。平台实际的文件权限、网络权限和审批要求必须遵守；技术上可执行不代表任务已授权，不得绕过或自行重建平台权限机制。
- 用户中途询问进度、提出问题或补充相容要求时，答复并继续原目标。只有明确取消或替换目标才停止后续不相容工作。
- 收到取消指令后核对已经启动的操作，并使用可用且已授权的停止或恢复手段；保留仍有效的工作和证据。依据工具结果确认停止或撤销，不能将“收到取消请求”写成“操作已停止”。
- 失败后继续范围内的安全恢复；重试次数本身不构成新审批条件。涉及新增费用、不可重复的外部效果或累计限制时，按实际影响与原授权处理。

## 4. 工作组织与计划

- 小而明确的任务适合 `xhigh`。复杂任务按形态建议 Max 或 Ultra：Max 适合需要连贯、深入推理的紧密关联问题；Ultra 适合拆分方式会变化、多个独立方向需要协调的工作。
- 推理模式和子代理数量分别判断。两种模式都可合理委派和并行修改；按独立问题、验收和恢复边界划分职责，不固定代理数量，也不默认只允许一个代理写入。
- 仅在反复相互破坏或验证持续僵持等实际问题出现时，临时集中修改责任；问题修复并验证后恢复正常协作。模式选择不代表正确性、权限或完成证据，不宣称已切换无法核实的模型设置。
- 计划阶段统一使用数字分隔，例如 `stage2.1.4`；字母仅用于附加项，例如 `stage2.1.4A`。
- 普通有限任务使用简短计划。复杂阶段围绕一个可验证结果和一致的验收、恢复边界安排；独立结果可分别验收和恢复时再拆分。
- 子计划或分支计划开头必须写明 `Source plan`（来源计划）、`Derived from`（衍生来源）、范围、不涉及的内容、保留状态、进入条件和退出标准。
- 仅当多层或并行计划使当前入口不明确时建立计划索引，并先查询索引再读相关计划。索引只负责定位，不能授予权限；完成时同步状态及后续归属，保留不可改写的证据。
- Graph（关系图）仅按实际任务需要采用。若未来使用，原始文件仍是权威来源；重建需求、是否已授权、是否执行和验证结果应按实际状态分别报告，已授权的重建直接继续。

## 5. 项目边界与数学约定

- 当前计算在浏览器内运行，工作区状态存于 `localStorage`，导出由用户触发。修改存储格式、读取或清除持久数据时，明确兼容性和恢复方式。
- 保留 Negative Binomial（负二项分布）约定：`r` 为所需成功次数，`p` 为单次成功概率，随机变量为第 `r` 次成功前的失败次数；源码参数键为 `prob`。
- 当前二维视图使用同一分布族的独立 X/Y，参数可分别设置；联合密度和分布函数均为边缘函数的乘积。改变独立性假设需要明确约定。
- 保留 VaR（风险价值）的分位数约定和 ES（预期损失）的分位数积分定义，包括离散边界概率质量及尾部一阶矩不存在时的发散结果。详细数值约定见 README 和对应测试。
- 数据参考值保留 SciPy 来源，不为让测试通过而直接改写参考结果或放宽容差；修改数学假设时先解释并取得一致意见。
- commit、push、部署和启用分别按授权范围判断。push 授权仅覆盖约定的仓库和分支，不自动包含强制推送、部署配置、域名、生产数据或账户操作。
- 部署前核实远端仓库、托管项目、域名归属及已有自动部署设置，按实际外部效果和已有授权执行。不得根据同名域名假定归属。
- 不提交凭据、`.venv/`、`node_modules/`、`dist/` 或其他已忽略的本地产物。

## 6. 验证与交付

- Python 优先使用项目 `.venv/bin/python`；前端使用 `package.json` 中的 npm 命令及项目依赖。不要为普通验证重建环境或升级依赖。
- 按受影响的结论选取最小有效验证。应用或数学行为变更使用 `npm test`；类型、依赖或构建相关变更使用 `npm run build`；界面交互变更补充对应浏览器检查。
- 纯文档变更检查事实、版本、链接和 `git diff --check`。仅在确需更新数值参考时运行 `.venv/bin/python scripts/generate_scipy_reference.py`。
- 复用仍适用的通过证据；只有相关输入、代码、约定、验证方式、验收条件或环境变化才重新验证对应结论。若未来有汇总命令覆盖子检查，正常只运行汇总命令一次。
- 验收证据充分后停止追加重复检查。不因 skill 升级新增无实际用途的检查脚本、钩子、图文件或审批流程。
- 交付前审查 diff；提交时只暂存任务内文件。已授权 push 时核对远端状态，以普通 push 推送，随后核实远端提交与本地结果相符。
- 结尾报告实际修改、关键文件、通过或未执行的验证和重要限制，明确区分已检查、已修改、已验证、已提交、已 push、已部署和已启用。
